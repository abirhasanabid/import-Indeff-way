const sum = (first, second) => {
    return (first + second);
};

const multiply = (first, second) => {
    return (first * second);
}
 const divided = (first,second)=>{
    return (first/second);
 }


export {sum,multiply,divided}